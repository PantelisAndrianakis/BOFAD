public class Bad {
    int a, b;
	var x = compute();
	//missing space
	@Nullable
	String name;
	// Fallback — retry once.
	void run()
	{
		items.stream().forEach(item -> use(item));
		switch (kind)
		{
			case A -> handle();
		}
	}


	int c;   
}
