public class Bad {
    int a, b;
	var x = compute();
	//missing space
	@Nullable
	String name;
	// Fallback — retry once.
	void run()
	{
		items.stream().forEach(item -> use(item));
		switch (kind)
		{
			case A -> handle();
		}
	}


	int c;   
	auto speed = compute();
	String s1, s2;
	private int count;
	static final int maxRetries = 3;
	java.util.List things;

	/** Doc with the {@link Bad} form. */
	void moreBad()
	{
		if (ready) fire();
		int i = 0; i++;
		// lowercase fragment comment
		// Retry once, twice, and thrice.
	}
}

interface IBadThing
{
}
